#pragma once
#include <eosio/eosio.hpp>
#define HISTORY_DAPPSERVICE_ACTIONS_MORE()

SVC_RESP_STORAGE(hststore)(uint32_t size, name current_provider){
}

SVC_RESP_STORAGE(hsthold)(uint32_t size, name current_provider){
}

SVC_RESP_STORAGE(hstserve)(uint32_t size, name current_provider){
}



SVC_RESP_STORAGE(hstreg)(uint32_t size, name current_provider){
}