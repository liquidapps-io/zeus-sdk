#pragma once
#include <eosio/eosio.hpp>
#define HISTORY_DAPPSERVICE_ACTIONS_MORE()

SVC_RESP_HISTORY(hststore)(uint32_t size, name current_provider){
}

SVC_RESP_HISTORY(hsthold)(uint32_t size, name current_provider){
}

SVC_RESP_HISTORY(hstserve)(uint32_t size, name current_provider){
}



SVC_RESP_HISTORY(hstreg)(uint32_t size, name current_provider){
}